# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust / Tauri build artifacts
/src-tauri/target

# Catálogos e importaciones (pueden pesar cientos de MB — no van al repo)
*.dccat
*.dccat-wal
*.dccat-shm
*.dcmf
*.dcmd

# Secrets
.env
.env.*

# ffmpeg/ffprobe sidecars (estáticos, pesados — se bajan con scripts/fetch-ffmpeg.sh)
/src-tauri/binaries/

# Sitio web (vive en la rama feat/website). Artefactos de build y link a Vercel:
# nunca van al repo — un merge los coló una vez y metió 80 MB de basura.
/site/.next/
/site/.vercel/
/site/next-env.d.ts
